---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="character strengths quiz — the keepers."
  description="Character strengths inventory tied to a keepers reading list."
  canonical="https://sftimes.com/quizzes/strengths"
  current="quizzes"
>
  <link rel="stylesheet" slot="head" href="/quizzes/quiz.css" />

  <section class="qz-sub-hero container">
    <p class="ui-label">quiz №7 · the identify series</p>
    <h1>what's your strongest <em>beat</em>?</h1>
    <p class="qz-sub-hero__deck">
      Twenty-four character strengths from the VIA framework, ranked. We name your top five and write you a reading list of saturday letters that match.
    </p>
    <p class="qz-sub-hero__meta">24 items · ~4 minutes · one page.</p>
  </section>

  <div class="quiz-page container">
    <noscript>
      <p class="qz-sub-noscript">This quiz needs JavaScript. Please enable it to take the inventory.</p>
    </noscript>
    <div id="quiz-root"></div>
    <p class="qz-sub-foot">Based on the VIA Inventory of Strengths. For curiosity, not diagnosis.</p>
  </div>
</BaseLayout>

<script src="/quizzes/strengths.config.js" is:inline></script>
<script src="/quizzes/quiz-engine.js" is:inline></script>

<style>
  .qz-sub-hero { padding-top: var(--s-9); padding-bottom: var(--s-6); border-bottom: 1px solid var(--rule); }
  .qz-sub-hero .ui-label { color: var(--ink-soft); margin-bottom: var(--s-4); }
  .qz-sub-hero h1 {
    font-family: var(--font-display);
    font-weight: 400;
    font-size: clamp(2.5rem, 5.5vw, 4rem);
    line-height: 1;
    letter-spacing: -0.022em;
    margin: 0 0 var(--s-4);
    max-width: 22ch;
    font-variation-settings: 'opsz' 144, 'SOFT' 30;
  }
  .qz-sub-hero h1 :global(em) {
    font-style: italic;
    color: var(--terracotta);
    font-variation-settings: 'opsz' 144, 'SOFT' 80, 'WONK' 1;
  }
  .qz-sub-hero__deck {
    font-family: var(--font-body);
    font-size: clamp(1rem, 1.4vw, 1.25rem);
    line-height: 1.55;
    color: var(--ink-soft);
    max-width: 56ch;
    margin: 0 0 var(--s-4);
  }
  .qz-sub-hero__meta {
    font-family: var(--font-ui); font-size: 0.75rem; letter-spacing: 0.14em; color: var(--fog); margin: 0;
  }
  .quiz-page { padding-block: var(--s-7) var(--s-9); }
  .qz-sub-noscript {
    font-family: var(--font-body); color: var(--ink-soft);
    padding: var(--s-5); background: var(--paper);
    border-left: 3px solid var(--terracotta);
  }
  .qz-sub-foot {
    margin-top: var(--s-7); font-family: var(--font-ui);
    font-size: 0.75rem; letter-spacing: 0.08em; color: var(--fog);
  }
</style>
