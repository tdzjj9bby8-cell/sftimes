---
import BaseLayout from '../layouts/BaseLayout.astro';
import PageMasthead from '../components/PageMasthead.astro';
const corrections: Array<{date: string; story: string; note: string}> = [];
---
<BaseLayout title="corrections — the keepers." description="Every correction we have made, dated and named." current="corrections">

  <PageMasthead
    title="corrections."
    eyebrow={`${corrections.length} corrections to date · dated · named · described`}
    subscript="THE KEEPERS"
  />

  <div class="container container-narrow page">
    <p class="lead">what we got <em>wrong</em>, and what we changed. Every correction is dated. The story is named. The change is described. This page exists because we will eventually make a mistake, and the alternative to a public log is hiding it.</p>
    {corrections.length === 0
      ? <p class="corrections-empty"><em>no corrections to date.</em></p>
      : <ul class="corrections">{corrections.map((c) => (
          <li><strong>{c.date}</strong> · <a href="#">{c.story}</a><p>{c.note}</p></li>
        ))}</ul>}
  </div>
</BaseLayout>

<style>
  .page { padding-block: var(--s-9); }
  .page h1 {
    margin-block: var(--s-3) var(--s-5);
    font-variation-settings: 'opsz' 144, 'SOFT' 30;
  }
  .page h1 :global(em) {
    font-style: italic;
    color: var(--terracotta);
    font-variation-settings: 'opsz' 144, 'SOFT' 80, 'WONK' 1;
  }
  .page .lead { font-size: 1.125rem; color: var(--ink-soft); margin-bottom: var(--s-6); max-width: 56ch; }
  .corrections { list-style: none; padding: 0; }
  .corrections li { padding-block: var(--s-4); border-bottom: 1px solid var(--rule); }
  .corrections-empty {
    font-family: var(--font-display);
    font-style: italic;
    font-size: 1.5rem;
    color: var(--fog);
    margin-top: var(--s-6);
    font-variation-settings: 'opsz' 144, 'SOFT' 80, 'WONK' 1;
  }
</style>
